# Read parquet files from j 

Intended eventually to have a similar interface to the `tables/csv` addon.

## Dependencies

* libarrow-glib
* libparquet-glib

See instructions here: https://arrow.apache.org/install/
